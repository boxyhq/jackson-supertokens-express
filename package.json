{
  "name": "jackson-supertokens-express",
  "version": "0.1.0",
  "private": true,
  "description": "SAML Jackson + Supertokens demo app",
  "keywords": [
    "SAML 2.0"
  ],
  "repository": {
    "type": "git",
    "url": "https://github.com/boxyhq/jackson-supertokens-express.git"
  },
  "license": "Apache 2.0",
  "scripts": {
    "dev": "docker-compose -f ./docker-compose.yml up -d --build",
    "dev-destroy": "docker-compose -f ./docker-compose.yml down --volumes --remove-orphans"
  },
  "dependencies": {
  },
  "devDependencies": {
  },
  "engines": {
    "node": ">=14.18.1"
  }
}